# 《OpenCode 深入解析：代理迴圈、工作階段、工具與擴充優先架構》

> **版本基線：** OpenCode 1.18.25（2026-09）。OpenCode 與模型供應商更新很快；執行指令、模型 ID 與設定欄位應以 [官方文件](https://opencode.ai/docs) 及 `opencode --help` 為準。

## 讀者服務與範例資源

本書範例、勘誤、更新與讀者資源位於：

<https://github.com/t945935/opencode-deep-dive>

```bash
git clone https://github.com/t945935/opencode-deep-dive.git
```

請勿把 API Key、存取權杖、客戶資料或生產環境設定提交至此儲存庫。

## 前言

OpenCode 是可在終端機、服務或其他介面中使用的 AI 程式設計代理。它將模型供應商、工作階段、工具、權限與擴充能力拆開：核心維持小而穩定，能力則透過設定、MCP、Plugin、Agent 與外部介面組合。這使它適合從單人互動式程式開發，延伸到可嵌入產品的代理服務。

本書採「先使用、再理解、後擴充」的順序。每一章都應以小型 Git 專案實作；讓代理修改程式後，仍須由人員閱讀 diff、執行測試並決定是否提交。

# 第一部　OpenCode 基礎與整體架構

## 第 1 章　OpenCode 是什麼

OpenCode 的工作不是單純補全程式碼，而是協調一段可重複的代理迴圈：接收使用者目標、讀取目前上下文、向模型請求下一步、執行獲准的工具，再把結果交回模型。模型負責推理與產生文字；OpenCode 負責讓推理可以安全地接觸檔案、終端機與外部服務。

理解本書的五個角色：**Provider** 提供模型；**Session** 保存對話與工作狀態；**Tool** 執行具體動作；**Skill／Command** 保存可重用工作流程；**MCP／Plugin** 將能力接到核心外部。它們不是互斥選項：Skill 可要求模型使用 Tool，而 MCP 可提供新的 Tool。

**練習：** 在一個 Git 專案內執行 `opencode`，要求它先說明目錄結構，不要修改檔案。觀察它讀取了哪些內容。

## 第 2 章　安裝、啟動與基本操作

安裝方式隨平台改變，先依官方安裝頁完成安裝，接著確認：

```bash
opencode --version
opencode --help
opencode providers
opencode models
```

在專案根目錄啟動 `opencode` 可進入互動式介面；`opencode run "任務"` 適合腳本或一次性任務。首次操作請選擇安全的小目標，例如「列出測試指令並解釋，不要執行」。確認模型、認證與工作目錄正確後，再允許讀檔、執行測試或修改檔案。

建立 `.gitignore`、乾淨的 Git 狀態與可執行測試，是使用代理前最有效的保護。代理不應取代版本控制；每次可接受的修改都應保留為可審查的 diff。

## 第 3 章　從請求到回應：Agent Loop

一次代理工作通常依序經過：

1. 使用者提出目標與限制。
2. OpenCode 建立或恢復 Session，組合系統指示、專案規則與訊息歷史。
3. Provider 將上下文送入模型。
4. 模型直接回答，或要求呼叫一項工具。
5. OpenCode 依權限政策執行工具，將輸出寫回 Session。
6. 模型根據工具結果決定下一步；直到任務完成、遭中斷或失敗。

工具輸出也是上下文，過長的建置日誌會降低後續推理品質。因此工具應回傳足以判斷下一步的資料，而非無限制傾倒所有輸出。可將大任務拆成「探索、計畫、修改、測試、審查」五步，並要求每一步產生可驗證結果。

## 第 4 章　設定檔放哪裡

設定可分成使用者層與專案層。實際搜尋順序與支援檔名請以當前官方文件為準；常見做法是將個人偏好放在使用者設定目錄，例如 Linux 的 `~/.config/opencode/opencode.jsonc`，將可共享的專案規則放在專案根目錄的 `opencode.json` 或 `opencode.jsonc`。

`AGENTS.md` 適合放人類可讀的專案工作規則，例如測試指令、程式風格與不可修改的目錄。自訂 Agent、Command 或 Plugin 則應依官方規範置於專案的 `.opencode/` 結構中。不要把個人 Token、絕對路徑或機密設定寫進版控檔案。

設定的基本原則是：預設值最廣、專案設定較具體、命令列選項只用於當次工作。發生「為何模型不是我選的」時，先檢查命令列 `--model`、專案設定與全域設定是否同時存在。

# 第二部　設定、模型與上下文

## 第 5 章　LLM Provider 與模型選擇

OpenCode 不是模型供應商。Provider 是提供模型與帳務的服務，模型 ID 則識別特定可用模型。先以 `opencode providers` 完成登入或憑證設定，再用 `opencode models [provider]` 查詢當前可用模型。模型可透過 `-m provider/model` 在單次啟動時覆寫，也可在設定檔指定預設值。

初學者只需選一個主力模型。一般程式開發可優先挑選能力平衡的 Claude Sonnet、OpenAI GPT 或 Gemini Pro 系列中，帳號可用且預算可接受者；不要把書中的示例 ID 當成永久名稱。選擇時看四件事：程式與工具使用品質、回應速度、長上下文成本、原始碼是否可傳送至該 Provider。

第二個模型的最佳用途是備援或審查，而不是隨機切換。可用較低成本模型處理摘要與小型整理，把跨模組重構、疑難除錯交給主力模型。Provider 暫時失敗時，保留 Session、記錄已執行工具與測試結果，再有意識地切換備援模型。

範例設定位於 [`resources/opencode.example.jsonc`](resources/opencode.example.jsonc)。其中的 `provider/model-name` 必須改為你的帳號實際可用 ID；API Key 不可放入此檔。

## 第 6 章　Session、訊息樹與上下文

Session 是一次工作可恢復的記錄，包含使用者訊息、助手回覆、工具呼叫及結果。`opencode --continue` 可繼續最近一次 Session；`--session <id>` 指定特定 Session；`--fork` 則在繼續時建立分支，適合比較兩種解法而不污染原本歷史。

上下文不是無限的。當對話、檔案內容與工具日誌累積到模型視窗上限，OpenCode 必須壓縮、摘要或捨棄較早資訊。長任務要主動留下可讀的交接點：目標、已修改檔案、未完成工作、測試結果與風險。把這些寫入短摘要，比要求模型記住整個終端機日誌可靠。

## 第 7 章　中斷、停止與重新開始

中斷不等於失敗。使用者可在不安全、偏離目標或成本過高時停止代理；先檢查 Git diff，再決定保留、還原或提交現有變更。若工具正在執行，終止後應確認沒有留下背景程序、鎖檔或部分寫入的檔案。

重新開始前，先回答三個問題：目前工作樹是否乾淨？最後成功的測試是什麼？要繼續原 Session、以 `--fork` 分支，還是建立新 Session？有明確答案時，恢復工作會比盲目重試更安全。

# 第三部　Prompt、Skills、Tools 與 MCP

## 第 8 章　Prompt Templates 與 Skills

Prompt Template 將常用指令文字參數化；Skill 則把某類工作所需的步驟、限制與驗收條件封裝起來。好的 Skill 不會只說「修好 bug」，而會說明：先重現問題、最小化修改、執行指定測試、回報 diff 與風險。

初學者可先建立三個可重用流程：程式碼導覽、測試失敗分析、變更審查。每個流程都應限制工具範圍，並要求代理在修改前先提出計畫。Skill 與 Prompt 是指導模型的文件，不是安全邊界；真正的安全仍須由工具權限與人工審查提供。

## 第 9 章　常用 Skills 實戰

最常用的 Skill 可分為：

- **導覽：** 找入口檔、相關測試與資料流，先不修改。
- **除錯：** 重現錯誤、提出假設、加入或更新測試、最小修正。
- **重構：** 列出不變條件與預期 diff，逐步修改並測試。
- **審查：** 依正確性、安全性、測試與可維護性分類問題。
- **Git 摘要：** 根據已審查的 diff 產生提交說明，不自行提交。
- **文件：** 從程式與測試萃取 README 或 API 說明，標出無法驗證的敘述。

把每個 Skill 的輸入、允許工具、成功條件與失敗處理寫清楚，能減少看似流暢但不可驗證的回覆。

## 第 10 章　內建工具與自訂工具

工具將模型的意圖轉成副作用：讀寫檔案、搜尋程式碼、執行命令或呼叫服務。工具設計的首要原則是最小權限與可預測輸入。輸入應使用明確型別，輸出應包含成功狀態、必要摘要與可診斷錯誤；避免讓模型自由組合危險 Shell 字串。

自訂工具應先在隔離測試環境驗證，再提供給代理。針對破壞性操作，改用 preview／dry-run、路徑白名單、明確確認與可還原操作。工具失敗時回傳可行的下一步，而不是將機密或龐大日誌原封不動放進上下文。

## 第 11 章　MCP 外部工具整合

MCP（Model Context Protocol）讓 OpenCode 以標準協定連接外部工具與資料來源，例如 GitHub、文件搜尋、資料庫或內部服務。它適合把能力留在獨立服務，而非把供應商 SDK 寫入每個 Agent。

導入 MCP 前要檢查：伺服器來源、授權範圍、可讀寫資源、網路邊界、日誌是否含敏感資料，以及失敗時的行為。先以唯讀、單一服務與測試帳號開始。MCP 是能力介面，不會自動讓工具安全；權限與使用者確認仍不可省略。

# 第四部　Extension-First 設計

## 第 12 章　事件與生命週期

代理系統的事件包括 Session 建立、模型請求、工具開始、工具完成、錯誤與中斷。事件可用於記錄、指標、權限檢查與 UI 更新。事件處理器不應改變核心語意或阻塞主迴圈；長時間工作應非同步處理，並提供逾時與失敗紀錄。

觀測資料至少應包含 Session ID、模型、工具名稱、耗時、結果狀態與 token／成本資訊；避免將完整 Prompt、原始碼或憑證直接寫進集中式日誌。

## 第 13 章　Extension 架構

Extension-first 的意思不是「所有功能都做成外掛」，而是核心只保留穩定協調能力，領域功能透過明確 API 加入。這降低核心耦合，也讓團隊能替換 Provider、UI 或工具實作。

好的 Extension 有小而穩定的介面、清楚的設定、可測試的副作用與明確版本相容範圍。它不應依賴未公開的內部狀態。升級 OpenCode 前，先在測試專案驗證關鍵 Extension，並鎖定或記錄已驗證版本。

## 第 14 章　實用擴充模式

常見擴充模式包括：自訂 Command 封裝固定工作、專用 Agent 設定角色與權限、Provider Adapter 接入內部模型閘道、事件 Hook 加入審計，以及 MCP Server 連接業務服務。先從一個痛點開始，例如「每次 PR 都要手動整理測試結果」，再決定它應是 Prompt、Skill、Tool、Plugin 還是 MCP。

選錯抽象層會增加維護成本。只改提示文字時不要寫 Plugin；需要受控副作用時不要只寫 Prompt；要與多種客戶端共用時，MCP 或 SDK 往往比 UI 專屬功能更合適。

## 第 15 章　替換終端機 UI

TUI 是一種客戶端，不是 OpenCode 的唯一形式。替代介面需處理輸入、串流輸出、工具確認、Session 列表、錯誤顯示與取消操作。不要把核心業務邏輯放在畫面元件中；UI 應透過穩定的服務或事件介面讀取狀態。

對自動化流程，可採用 `opencode run` 或 headless server；對 Web 或桌面應用，應特別處理身分驗證、跨站請求、租戶隔離與取消請求。任何 UI 都必須清楚顯示代理將執行的副作用。

# 第五部　應用程式整合與發佈

## 第 16 章　SDK 整合

SDK 適合將 Agent 功能嵌入既有應用程式。整合時先定義產品層的責任：誰建立 Session、誰保存訊息、哪些工具可用、如何顯示串流事件、何時要求人工批准。不要把使用者原始輸入直接傳給具有高權限工具的 Agent。

最小整合應有：建立／恢復 Session、傳送訊息、訂閱回覆與工具事件、取消工作、取得最終結果，以及記錄可追蹤的請求 ID。先在單一使用者、唯讀工具的情境驗證，再逐步加入寫入能力。

## 第 17 章　JSONL RPC

JSONL RPC 以每行一個 JSON 訊息交換請求、回覆與事件，適合不同語言或程序之間串接。設計 Client 時，應以 request ID 對應回覆、處理串流事件、設定逾時與取消、驗證 JSON 結構，並在連線中斷後決定是否可安全重送。

RPC 邊界應只傳遞必要資料。大型檔案、秘密與未經驗證的工具輸出應避免直接塞入訊息。為協定定義版本欄位與錯誤碼，可讓客戶端與伺服器獨立升級。

## 第 18 章　Package 發佈與維護

發佈前檢查 Package 名稱、授權、依賴版本、設定說明、最小範例與安全聲明。安裝說明必須包含移除與升級方式；若 Extension 需要外部帳號或權限，也應在安裝前說明。

維護者應提供變更紀錄、相容性矩陣與問題回報模板。不要用「latest」取代測試策略：讀者可以使用 latest，但專案 CI 應針對已知版本驗證。

## 第 19 章　完整實戰專案

完整專案可做成「受控的程式庫維護助理」：它先讀取 `AGENTS.md`，僅允許在專案目錄內搜尋與執行測試；收到 bug 後先重現、提出計畫、取得確認才修改；完成後回報檔案清單、測試結果與剩餘風險。

實作順序是：建立乾淨測試專案、設定單一模型、加入專案規則、驗證唯讀導覽、加入測試工具、最後才加入寫檔與 MCP。每加一層能力，都要新增失敗案例與權限測試。

# 第六部　工具選擇與設計哲學

## 第 20 章　OpenCode 與主流工具比較

OpenCode 適合偏好終端機、需要多 Provider 選擇、希望以 MCP／Plugin／SDK 客製整合的開發者。Claude Code 與 Aider 也適合終端機工作流；Cursor、Windsurf 等 IDE 工具則適合需要圖形化編輯、補全與內建視覺操作的使用者；GitHub Copilot 適合已深度使用 GitHub 生態的團隊。

沒有工具能免除工程判斷。若需求只是 IDE 補全，建立 Extension-first 平台通常過度複雜；若要把代理嵌入產品、控制 Provider 與工具生命週期，單一 IDE Agent 則可能不夠。以工作流、治理與整合需求選擇，而非只比較模型名稱。

## 第 21 章　最佳實務與安全性

將代理視為能執行動作的軟體，而不是聊天視窗。使用最小權限、明確確認、沙箱、可回復 Git 工作流與秘密管理。針對提示注入，將外部內容視為不可信資料，不允許文件、Issue 或網頁自行提升工具權限或改寫系統規則。

品質控制包含單元測試、整合測試、代理評測案例與人工 diff 審查。可觀測性包含模型、成本、工具成功率、取消率與任務完成率；衡量前先移除敏感內容。對昂貴或高風險任務設定 token、時間與工具次數上限。

## 第 22 章　OpenCode 的設計哲學

精簡核心的價值在於可替換：模型會改變、UI 會改變、工具與工作流程也會改變，但 Session、訊息、權限與事件邊界可以保持可理解。Extension-first 並非追求無限功能，而是讓每個能力有清楚的責任、依賴與失敗模式。

成功的代理系統不是讓模型獲得最多權限，而是讓人員能理解、檢查、停止、恢復與替換它。當模型能力持續進步，這些工程邊界會比任何單一模型名稱更持久。

# 附錄 A　常用指令速查

```bash
opencode --help
opencode providers
opencode models
opencode run "說明此專案的測試方式"
opencode --continue
opencode --session <session-id> --fork
opencode stats
opencode export <session-id>
opencode import <file>
opencode mcp --help
opencode agent --help
opencode debug --help
```

# 附錄 B　提交前檢查表

- [ ] 不含 API Key、Token、客戶資料或機密日誌。
- [ ] 已閱讀代理的 Git diff。
- [ ] 已執行相關測試，並記錄結果。
- [ ] 自訂 Tool 具備最小權限與錯誤處理。
- [ ] MCP 的來源、授權範圍與資料流已審查。
- [ ] 設定、模型 ID 與 OpenCode 版本已記錄。
- [ ] 讀者資源已更新：<https://github.com/t945935/opencode-deep-dive>。
