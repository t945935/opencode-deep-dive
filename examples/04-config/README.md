# 第 4 章：設定檔

將 `resources/opencode.example.jsonc` 複製到測試專案根目錄：

```bash
cp resources/opencode.example.jsonc ./opencode.jsonc
```

把模型佔位值換成 `opencode models` 顯示的可用 ID。API Key 不得寫入檔案或提交 Git；設定檔路徑與優先順序請以當前版本官方文件為準。
