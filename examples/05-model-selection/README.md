# 第 5 章範例

1. 複製 `../../resources/opencode.example.jsonc` 至專案根目錄的 `opencode.jsonc`。
2. 將 `provider/model-name` 替換為 `opencode models` 顯示、且帳號可用的模型 ID。
3. 將憑證以 `opencode providers` 管理；不要寫入設定檔或 Git。
4. 執行 `opencode run "說明目前專案的測試指令"` 驗證連線。
